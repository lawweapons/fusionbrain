# API-mode clients only need these:
requests>=2.32
python-dotenv>=1.0
yt-dlp>=2025.1.1
youtube-transcript-api>=1.0
pypdf>=5.1
beautifulsoup4>=4.12
markdown-it-py>=3.0

# DB-mode (direct Postgres insert, for running on the FusionBrain VPS):
psycopg[binary]>=3.2
